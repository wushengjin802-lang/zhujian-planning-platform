# 工作台功能增强说明（2026-07-03）

## 本次完成

1. 新增后端工作台聚合服务 `app/services/dashboard.py`。
2. 新增认证接口 `GET /api/dashboard`，支持当前项目和全部项目两种范围。
3. 工作台页面增加项目综合指标、流程进度、动态待办、待审核队列、风险提醒、异步任务中心、快捷入口、近期活动和项目组合概览。
4. 工作台数据与资料、事实、章节、质量问题、投资测算、成果、后台任务及审计日志联动。
5. 增加工作台后端聚合单元测试。

## 当前边界

- 待办、审核队列和通知由现有业务数据实时派生，尚未建立独立持久化实体。
- 暂未支持工作项领取、转交、截止时间和独立关闭。
- 暂未支持后台任务取消与重试。
- 角色视角已能区分管理/个人展示模式，但项目成员级权限仍需后续完善。

## 验证

- Vue TypeScript 类型检查通过。
- Vite 生产构建通过。
- Python compileall 通过。
- 工作台聚合单元测试通过。
- FastAPI 路由导入验证通过。
