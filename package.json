{
  "name": "zhujian-planning-platform",
  "version": "2.2.0",
  "private": true,
  "workspaces": [
    "apps/web-vue",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run api:dev\" \"npm run web:dev\"",
    "api:dev": "cd apps/api-py && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8787",
    "api:migrate": "cd apps/api-py && python -m alembic upgrade head",
    "worker:dev": "cd apps/api-py && celery -A app.worker.celery_app.celery_app worker -Q parse,ai,export,calc --loglevel=INFO",
    "web:dev": "npm run dev -w apps/web-vue",
    "build": "npm run build -w apps/web-vue",
    "typecheck": "npm run typecheck -w apps/web-vue"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  }
}
